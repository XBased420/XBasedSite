import { services, projects, skills } from './content.mjs';
export const escape = value => String(value).replace(/[&<>"']/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]));
const cta = (label = 'Start a project', style = '') => `<a class="button ${style}" href="#booking">${label}<span aria-hidden="true">↗</span></a>`;
const contact = (label = 'Email Xavier') => `<a class="email-link" href="#email-fallback">${label}</a>`;
const field = (id, label, options = {}) => `<div class="field ${options.wide ? 'wide' : ''}"><label for="${id}">${label}${options.required ? ' <span aria-hidden="true">*</span>' : ' <span class="optional">Optional</span>'}</label>${options.textarea ? `<textarea id="${id}" name="${id}" rows="5" maxlength="4000"` : `<input id="${id}" name="${id}" type="${options.type || 'text'}" maxlength="${options.max || 200}" ${options.autocomplete ? `autocomplete="${options.autocomplete}"` : ''}`} ${options.required ? 'required' : ''} aria-describedby="${id}-error" ${options.type === 'tel' ? 'inputmode="tel"' : ''}>${options.textarea ? '</textarea>' : ''}<span class="field-error" id="${id}-error"></span></div>`;
const select = (id, label, choices, required = false) => `<div class="field"><label for="${id}">${label}${required ? ' <span aria-hidden="true">*</span>' : ' <span class="optional">Optional</span>'}</label><select id="${id}" name="${id}" ${required ? 'required' : ''} aria-describedby="${id}-error"><option value="">${required ? 'Pick what feels closest' : 'Choose if you like'}</option>${choices.map(x => `<option>${escape(x)}</option>`).join('')}</select><span class="field-error" id="${id}-error"></span></div>`;
export function renderPage({ settings, site, base = '/' }) {
  const asset = path => `${base}${path}`;
  const canonical = `${site}${base.startsWith('/') ? base : '/'}`;
  const ready = Boolean(settings.endpoint);
  const config = JSON.stringify({ endpoint: settings.endpoint }).replace(/</g, '\\u003c');
  const schema = { '@context': 'https://schema.org', '@graph': [
    { '@type': 'Person', '@id': `${canonical}#xavier`, name: 'Xavier', url: canonical, sameAs: ['https://github.com/XBased420'] },
    { '@type': 'LocalBusiness', '@id': `${canonical}#business`, name: settings.brand, url: canonical, founder: { '@id': `${canonical}#xavier` }, areaServed: ['Dallas', 'Carrollton', 'Dallas–Fort Worth'], description: 'Websites and booking systems for small businesses and creatives in DFW and remotely.' },
    ...services.map(([name]) => ({ '@type': 'Service', name, provider: { '@id': `${canonical}#business` }, areaServed: 'Dallas–Fort Worth' }))
  ] };
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1">
  <title>XBased — Websites & booking systems | Xavier · DFW</title><meta name="description" content="Xavier builds websites and booking systems for small businesses, barbers, DJs, and artists in Dallas, Carrollton, and across DFW. Start a project.">
  ${!settings.launchReady ? '<meta name="robots" content="noindex, nofollow">' : ''}<link rel="canonical" href="${escape(canonical)}"><meta name="theme-color" content="#f5f6f8">
  <meta property="og:type" content="website"><meta property="og:title" content="XBased — Your business deserves a proper website."><meta property="og:description" content="Websites and booking systems, built by Xavier. DFW + Remote."><meta property="og:url" content="${escape(canonical)}"><meta name="twitter:card" content="summary"><meta name="twitter:title" content="XBased — Websites that put you to work"><meta name="twitter:description" content="Websites and booking systems for DFW businesses and creatives.">
  <link rel="icon" type="image/svg+xml" href="${asset('favicon.svg')}"><link rel="preload" href="${asset('assets/fonts/space-grotesk-latin.woff2')}" as="font" type="font/woff2" crossorigin><link rel="preload" href="${asset('assets/fonts/manrope-latin.woff2')}" as="font" type="font/woff2" crossorigin><link rel="stylesheet" href="${asset('styles.css')}"><script type="application/ld+json">${JSON.stringify(schema).replace(/</g, '\\u003c')}</script></head>
  <body><a class="skip-link" href="#main">Skip to content</a><header class="header wrap"><a class="wordmark" href="#" aria-label="XBased home">x<span class="wordmark-bracket">[</span>based<span class="wordmark-bracket">]</span><span class="wordmark-dot">.</span></a><nav aria-label="Main navigation"><a href="#work">Work</a><a href="#services">Services</a><a href="#about">About</a></nav>${cta()}</header>
  <main id="main"><section class="hero wrap" aria-labelledby="hero-title"><div class="hero-top"><p class="eyebrow">INDEPENDENT WEB DESIGN & DEVELOPMENT</p><p class="eyebrow">DFW + REMOTE</p></div><div class="hero-stage"><h1 id="hero-title">You do your thing.<br><span class="hero-blue">I’ll build the site.</span></h1><div class="type-mark" aria-hidden="true"><span>[</span><b>x</b><span>]</span></div></div><div class="hero-bottom"><p>Websites and booking systems for local businesses, artists, and people making something happen.<br><span class="intro-name">I’m Xavier. Let’s put your work online.</span></p><div>${cta()}<p class="small">Tell me what you have in mind.</p></div></div><div class="hero-foot"><span class="mono">DALLAS–FORT WORTH, TEXAS</span><a href="#work">A few things I’ve built <span aria-hidden="true">↓</span></a></div></section>
  <section id="work" class="section wrap"><div class="section-head"><div><p class="eyebrow">SELECTED WORK</p><h2>Real projects.<br>Different problems.</h2></div><p>Client work, works in progress, and things I built because I wanted them to exist.</p></div><div class="projects">${projects.map(p => `<details class="project reveal"><summary><div class="project-header"><span class="project-category mono">${escape(p.category)}</span><span class="badge ${p.status === 'LIVE' ? 'live' : ''}">${escape(p.status)}</span></div><div class="project-title"><h3>${escape(p.name)}</h3><span class="expand-icon" aria-hidden="true">+</span></div><p>${escape(p.line)}</p><span class="read-case">Read the case study <span aria-hidden="true">↗</span></span></summary><div class="case-content"><div class="case-columns"><div><h4>The problem</h4><p>${escape(p.problem)}</p></div><div><h4>What I built</h4><p>${escape(p.built)}</p></div></div><h4>Stack</h4><div class="chips">${p.stack.map(s => `<span>${escape(s)}</span>`).join('')}</div>${p.stackPending ? `<p class="placeholder">${escape(p.stackPending)}</p>` : ''}${p.screenshot ? `<img src="${asset(p.screenshot)}" alt="${escape(p.name)} website screenshot" width="1600" height="1000" loading="lazy" decoding="async">` : `<div class="screenshot-slot"><span class="mono">PROJECT SCREENSHOT</span><p>[[NEEDS XAVIER: screenshot — ${escape(p.name)}]]</p></div>`}${p.url ? `<a class="text-link" href="${escape(p.url)}" target="_blank" rel="noopener noreferrer">Visit the live site <span aria-hidden="true">↗</span></a>` : ''}</div></details>`).join('')}</div></section>
  <section id="services" class="section wrap"><div class="section-head"><div><p class="eyebrow">WHAT I CAN HELP WITH</p><h2>A good site.<br>A little less busywork.</h2></div><p>A website for your small business in DFW. A booking home for your next gig. Something that makes your day easier.</p></div><div class="services">${services.map(([name, description]) => `<article class="service reveal"><h3>${escape(name)}</h3><p>${escape(description)}</p></article>`).join('')}</div><div class="pricing"><div><p class="eyebrow">STARTING PRICES</p><h3>Know where you stand.</h3><p>We’ll agree on the scope and the quote before work starts.</p></div><div class="price-list">${[['One-page site', 'onePage'], ['Full site (up to 5 pages)', 'multiPage'], ['Booking system add-on', 'booking'], ['Maintenance', 'maintenance'], ['Deposit', 'deposit']].map(([label, key]) => `<div><span>${label}</span><strong>${settings.pricesApproved && settings.prices[key] !== null ? (key === 'deposit' ? `${settings.prices[key]}%` : `$${settings.prices[key]}${key === 'maintenance' ? '/mo' : ''}`) : '<span class="pending-price">To be confirmed</span>'}</strong></div>`).join('')}${!settings.pricesApproved ? '<p class="placeholder">[[NEEDS XAVIER: approve final price card]]</p>' : ''}</div></div></section>
  <section id="process" class="section process-section"><div class="wrap"><div class="section-head"><div><p class="eyebrow">HOW THIS WORKS</p><h2>One person.<br>A clear next step.</h2></div><p>You’ll talk directly with me, from the first message to the finished site.</p></div><ol class="steps"><li><span class="step-no mono">01 /</span><h3>Tell me the idea.</h3><p>Fill out the request below. Rough ideas are fine. “Not sure yet” is a real budget option.</p><span class="timing">At your pace</span></li><li><span class="step-no mono">02 /</span><h3>I’ll get back to you.</h3><p>Within 24 hours, I’ll email a few times to talk and a rough quote. We’ll work out the details together.</p><span class="timing">Within 24 hours</span></li><li><span class="step-no mono">03 /</span><h3>Agree. Then build.</h3><p>We settle the scope, cost, and schedule before I start. Then I build a version for you to review.</p><span class="timing ${!settings.processApproved ? 'placeholder' : ''}">${escape(settings.buildTiming)}</span></li><li><span class="step-no mono">04 /</span><h3>Review it. Go live.</h3><p>You review the site and send feedback. Once the agreed changes are finished, we get it online.</p><span class="timing ${!settings.processApproved ? 'placeholder' : ''}">${escape(settings.reviewTiming)}</span></li></ol></div></section>
  <section id="about" class="section wrap about"><div><p class="eyebrow">THE PERSON BEHIND THE SITE</p><h2>Hey, I’m Xavier.<br>I learn by building.</h2><span class="about-location mono">DFW + REMOTE</span></div><div class="about-copy"><p class="lead">I’ve been in restaurants since I was 16. I know what it’s like to have work to do and tools that get in the way.</p><p>When I worked as a personal assistant to a college professor in Long Beach, California, I started learning AI skills early. I took what I learned and put it to work on my own projects and business ventures.</p><p>I’m self-taught. No degree, no big agency story. I like making useful things and figuring out how to make them better.</p><p>If you’re looking for a web designer in Dallas or Carrollton, TX, I work with local businesses and creatives across DFW—and remotely, too.</p><div class="skills"><p class="eyebrow">WHAT I WORK WITH</p><div class="chips">${skills.map(s => `<span>${escape(s)}</span>`).join('')}</div></div></div></section>
  <section id="booking" class="section booking-section"><div class="wrap booking-layout"><div class="booking-intro"><p class="eyebrow">LET’S MAKE IT HAPPEN</p><h2>Start<br>a project<span>.</span></h2><p>Tell me about your business and what you need. You don’t need to know the technical stuff.</p><p>I’ll reply within 24 hours with a few times to talk and a rough quote.</p><p class="booking-note">Looking for a booking website for your barber business, DJ gigs, or artist projects in Dallas? Start here.</p>${contact('Rather send an email?')}<p class="email-fallback" id="email-fallback">Email: calipxj <span aria-label="at">[at]</span> gmail <span aria-label="dot">[dot]</span> com</p></div><div class="form-container">${!ready ? '<div class="setup-notice" role="note">Preview mode: requests aren’t connected yet. You can try the fields, or email Xavier.</div>' : ''}<form id="booking-form" novalidate><p class="form-key">Fields marked * are required.</p><div class="form-grid">${field('name', 'Your name', { required: true, autocomplete: 'name' })}${field('email', 'Email', { required: true, type: 'email', autocomplete: 'email' })}${field('phone', 'Phone', { required: true, type: 'tel', autocomplete: 'tel', max: 30 })}${field('business', 'Business name', { required: true, autocomplete: 'organization' })}${field('type', 'Type of business')}${select('hasSite', 'Do you have a site now?', ['Yes', 'No', 'Sort of'])}${field('needs', 'What do you need?', { required: true, textarea: true, wide: true })}${select('budget', 'What budget feels comfortable?', settings.budgets, true)}${select('timeline', 'When are you thinking?', ['ASAP', 'Few weeks', 'Few months', 'Just exploring'])}${field('socials', 'Your Instagram / socials')}${field('source', 'How’d you find me?')}</div><div class="honeypot" aria-hidden="true" inert><label for="website">Leave this empty</label><input id="website" name="website" type="text" tabindex="-1" autocomplete="off"></div><p class="small">Have photos or files? Email them after you submit.</p><p class="small">Your details are used to respond to this request. Please don’t include sensitive information.</p><div id="form-error" class="form-message" role="alert" hidden></div><button class="button submit" type="submit">Send project request <span aria-hidden="true">↗</span></button><p class="small">A request starts a conversation. It doesn’t commit you to a project.</p><noscript><p>Please enable JavaScript to use the request form, or use the email address beside it.</p></noscript></form><section id="form-success" class="form-success" hidden tabindex="-1" aria-labelledby="success-title"><span class="mono">NEXT STEP: XAVIER REVIEWS YOUR REQUEST</span><h3 id="success-title">Your request is on its way.</h3><p>I’ll review your request once it arrives and email you after I approve it.</p><p>No response within 24 hours? Check spam, then email me so I can check your request.</p>${contact('Email Xavier')}<button class="text-button" id="send-another" type="button">Send another request</button></section></div></div></section></main>
  <footer class="wrap footer"><a class="wordmark" href="#">x[based]<span class="wordmark-dot">.</span></a><p>Built by Xavier. DFW + Remote.</p><div>${contact()}<a href="https://github.com/XBased420" target="_blank" rel="noopener noreferrer">GitHub <span aria-hidden="true">↗</span></a><a href="#">Back to top ↑</a></div></footer><script type="application/json" id="site-config">${config}</script><script defer src="${asset('site.js')}"></script></body></html>`;
}
